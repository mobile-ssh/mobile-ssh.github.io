---
layout: ../../../layouts/DocLayout.astro
title: "Устранение неполадок"
description: "Руководство по устранению неполадок Mobile SSH: подключение, аутентификация, клавиатура, tmux, передача файлов и туннели."
---

# Устранение неполадок

На этой странице рассматриваются распространённые проблемы Mobile SSH и первые проверки, которые стоит выполнить перед изменением настроек SSH на стороне сервера.

## Не удаётся подключиться

Проверьте:

- На устройстве есть сетевой доступ.
- Имя хоста или IP-адрес сервера указаны верно.
- Порт SSH верен, обычно `22`.
- Брандмауэр, VPN, сеть оператора или сеть Wi-Fi не блокируют порт.
- SSH-сервер запущен и принимает подключения из сети, которую вы используете.

Если тот же хост работает с другого устройства, сравните точные хост, порт, имя пользователя, ключ и сетевой путь.

## Ошибка аутентификации

Проверьте:

- Правильность написания имени пользователя.
- Пароль или парольную фразу ключа.
- Разрешает ли сервер вход по паролю, по ключу или оба способа.
- Соответствует ли закрытый ключ открытому ключу в `authorized_keys` пользователя сервера.
- Был ли файл ключа импортирован полностью, включая строки заголовка и подвала.

Для зашифрованных закрытых ключей введите парольную фразу в поле пароля/парольной фразы.

## Не удалось импортировать закрытый ключ

Импорт закрытого ключа выполняется через системный файловый менеджер. Если импорт не удался:

- Убедитесь, что выбранный файл — закрытый ключ, а не открытый файл `.pub`.
- Откройте файл в доверенном текстовом редакторе и убедитесь, что он содержит полный блок ключа.
- Попробуйте вставить ключ вручную в поле закрытого ключа.
- Убедитесь, что тип ключа поддерживается: Ed25519, RSA, ECDSA или DSA на Android; Ed25519 или ECDSA (P-256/384/521) на iOS.

## Ввод с клавиатуры задерживается или изменяется

Mobile SSH отправляет нажатия клавиш прямо в оболочку с отключёнными автокоррекцией и предиктивными подсказками, поэтому клавиатура не должна переписывать текст до того, как он достигнет удалённой стороны. Если ваша клавиатура всё же изменяет ввод, убедитесь, что его не перехватывает системный инструмент замены текста или буфера обмена, и используйте ряд дополнительных клавиш для клавиш терминала, таких как `ESC`, `TAB`, `CTRL`, стрелки, `HOME`, `END`, `PGUP` и `PGDN`.

## Прокрутка tmux работает не так, как ожидалось

Mobile SSH меняет поведение прокрутки в зависимости от состояния терминала. В tmux или других программах с альтернативным экраном жесты прокрутки могут отправлять команды режима копирования tmux вместо прокрутки локальной истории. Если в tmux включён режим мыши, приложение отправляет escape-последовательности колеса мыши.

Если прокрутка работает неправильно:

- Попробуйте включить или отключить режим мыши tmux на удалённом сервере.
- Используйте `PGUP` и `PGDN` из ряда дополнительных клавиш.
- Дважды нажмите на панель для перехода в полноэкранный режим перед прокруткой плотного вывода.
- Отсоединитесь и снова подключитесь к tmux, если размер удалённого терминала выглядит устаревшим.

## Сессия оборвалась после блокировки экрана

На Android Mobile SSH использует keepalive, службу переднего плана, wake lock, Wi-Fi lock и попытки переподключения, чтобы уменьшить число разрывов соединения. Политики экономии батареи Android всё равно могут останавливать фоновую работу.

Проверьте:

- Отключите оптимизацию батареи для Mobile SSH, если ваше устройство агрессивно останавливает фоновые приложения.
- Поддерживайте стабильность Wi-Fi или мобильных данных во время долгих сессий.
- Снова откройте Mobile SSH и нажмите **Active Sessions** после разблокировки.
- Если сервер разорвал SSH-сессию, переподключитесь из недавних сессий.

На iOS система приостанавливает приложения в фоне, поэтому обычное SSH-соединение нельзя держать открытым бесконечно после переключения на другое приложение или блокировки экрана. Короткий льготный период покрывает быстрые переключения между приложениями; для более длительных перерывов включите **Auto-attach tmux session** в профиле сервера (или используйте транспорт **Eternal Terminal**), чтобы при переподключении вы возвращались в ту же оболочку, где остановились.

## Передача файлов не может просматривать файлы телефона

На новых версиях Android локальный просмотр файлов может требовать доступа к хранилищу. Выдайте доступ к хранилищу в настройках Android для Mobile SSH, затем снова откройте экран передачи файлов.

Если удалённые файлы загружаются, а локальные нет, SSH-подключение, вероятно, в порядке, а проблема заключается в локальном доступе к хранилищу Android.

На iOS разрешение на доступ к хранилищу не требуется: локальная панель показывает область документов приложения, а файлы добавляются через системные средства выбора документов и фотографий.

## Не удалось загрузить или скачать файл

Проверьте:

- SSH-сессия всё ещё подключена.
- Удалённый каталог существует.
- У удалённого пользователя есть права на чтение или запись по указанному пути.
- Локальный каталог назначения доступен для записи.
- На устройстве достаточно свободного места.
- Сеть стабильна для больших передач.

## Не удалось выполнить проброс порта

Проверьте:

- Локальный порт находится в диапазоне от `1` до `65535`.
- Локальный порт ещё не используется.
- Строка туннеля имеет вид `PORT` или `LOCAL:REMOTEHOST:REMOTE`.
- Удалённый хост и удалённый порт доступны с SSH-сервера.
- SSH-сервер разрешает проброс TCP.

## Журналы отладки

На стартовом экране есть кнопка **Debug**. Когда запись включена, Mobile SSH фиксирует диагностическую информацию о событиях терминала, объёмах данных SSH, сенсорном вводе, поведении при изменении размера и жизненном цикле туннелей. Остановите запись, чтобы сохранить архив отладки локально.

Проверяйте архивы отладки перед тем, как делиться ими. Они предназначены для устранения неполадок и могут содержать имена серверов, тайминги, поведение терминала или другие сведения об окружении.
