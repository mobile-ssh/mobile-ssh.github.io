---
layout: ../../../layouts/DocLayout.astro
title: "الطرفية"
description: "عناصر تحكم طرفية Mobile SSH، المفاتيح الإضافية، الأجزاء، التمرير، سلوك tmux، إجراءات النسخ، وإعدادات لوحة المفاتيح."
---

# الطرفية

صُمِّمت طرفية Mobile SSH للتشغيل على الهاتف والجهاز اللوحي. تجمع بين مساحة الطرفية، وصف المفاتيح الإضافية، وأجزاء الجلسات المتعددة، ومعالجة التمرير، وسلوك إعادة الاتصال.

## أساسيات الطرفية

- تستخدم الطرفية سلوكًا بنمط xterm مع دعم الألوان ومعالجة مفاتيح المؤشّر.
- يحتفظ مخزّن التمرير بما يصل إلى 5000 سطر.
- اضغط على جزء لتحديده قبل الكتابة.
- اقرص جزء الطرفية لتغيير حجم النص. يعيد Mobile SSH تحجيم الـ PTY البعيد بعد استقرار الإيماءة.
- اضغط مرتين على جزء (أو استخدم زر التوسيع الخاص به) للدخول إلى وضع ملء الشاشة. استخدم زر الرجوع على Android، أو زر الطي على iOS، للعودة إلى الشبكة.

## شبكة الجلسات المتعددة

يمكن لـ Mobile SSH تشغيل ما يصل إلى ثماني جلسات SSH في الوقت نفسه. تظهر كل جلسة كجزء في شبكة الطرفيات. يعرض رأس الجزء الوجهة أو العنوان الحالي. اضغط على جزء لتحديده، أو استخدم **+ Add Session** لبدء اتصال آخر.

إغلاق جزء يقطع جلسة SSH تلك. العودة إلى الشاشة الرئيسية يُبقي الجلسات النشطة متاحة عبر **Active Sessions**.

## صف المفاتيح الإضافية

على كلٍّ من Android وiOS، يظهر صف المفاتيح الإضافية فوق لوحة المفاتيح على الشاشة ويوفّر مفاتيح طرفية يصعب استخدامها على لوحات اللمس:

- `ESC`
- `TAB`
- `CTRL`
- `Shift`
- مفاتيح الأسهم
- `HOME`
- `END`
- `PGUP`
- `PGDN`
- تبديل لوحة المفاتيح

يعمل `CTRL` و`Shift` كمعدِّلَين لاصقَين للإدخال المتوافق التالي. على سبيل المثال، اضغط `CTRL` ثم اكتب `C` لإرسال Ctrl-C.

## سلوك لوحة المفاتيح

تستخدم الطرفية اتصالًا مباشرًا بالتمرير مع لوحة المفاتيح على الشاشة: يُرسَل كل حرف إلى الصدفة البعيدة أثناء كتابتك، مع إيقاف التصحيح التلقائي والاقتراحات التنبّؤية بحيث لا تعيد لوحة المفاتيح كتابة الإدخال قبل وصوله إلى الصدفة. يُبقي هذا Vim وtmux وhtop وless والأصداف ذات توليفات المفاتيح غير المعتادة وموجّهات كلمات المرور البعيدة قابلة للتنبؤ — فلا يوجد مخزّن اقتراحات لتعطيله.

- **اللمس على الطرفية لإظهار لوحة المفاتيح:** عند التفعيل، يطلب لمس الطرفية من النظام إظهار لوحة المفاتيح على الشاشة.

يبقى الإملاء الصوتي من لوحة المفاتيح الافتراضية يعمل: يُثبَّت النص المُملى مباشرةً في الصدفة كأي إدخال مكتوب آخر.

## لوحات المفاتيح العتادية

تقود لوحات المفاتيح الخارجية وBluetooth الطرفية مباشرةً على كلٍّ من Android وiOS. إلى جانب الأحرف العادية، يعيّن Mobile SSH مفاتيح الأسهم و`Home`/`End` و`PgUp`/`PgDn` و`Insert` و`Delete` و`Esc` ومفاتيح الوظائف `F1`–`F12` وتوليفات `Ctrl`+مفتاح و`Alt`/`Option` بوصفها Meta و`Shift`+`Tab`. تتّحد مفاتيح التعديل مع المعدِّلات اللاصقة في صف المفاتيح الإضافية.

## اللصق

يُغلَّف اللصق داخل برنامج يطلبه (bash وVim وغيرها من تطبيقات اللصق المُقوَّس) بعلامات اللصق المُقوَّس، بحيث يُدرَج محتوى الحافظة متعدد الأسطر كنص بدلًا من تنفيذه تلقائيًا سطرًا سطرًا. تُغلَّف عمليات اللصق الحقيقية فقط؛ أما النص المكتوب والمُملى فيبقى دون تغيير.

## التحديد والنسخ والمشاركة

اضغط مطوّلًا داخل الطرفية لتحديد الكلمة الواقعة تحت إصبعك، ثم اسحب المقابض لضبط التحديد. يوفّر شريط التحديد ثلاثة إجراءات:

- **Copy** — يضع النص المحدد في حافظة النظام. يبقى التحديد مظللًا بعد ذلك، فيمكنك مشاركته أو إعادة نسخه أو توسيعه.
- **Share** — يمرّر النص المحدد إلى ورقة مشاركة النظام (البريد، الملاحظات، المراسلة، إلخ).
- **Select all** — يوسّع التحديد ليشمل كامل مخزّن الطرفية المرئي، ثم انسخ أو شارك.

## البحث في الطرفية

ابحث في كامل مخزّن الطرفية — سجل التمرير والشاشة المرئية — وتنقّل بين النتائج. افتح البحث من عناصر تحكم الطرفية، واكتب استعلامًا، وتنقّل بين المطابقات. يعمل هذا على كلٍّ من أندرويد وiOS.

## الألوان والخط المائل والحافظة البعيدة

- تعرض الطرفية **ألوانًا حقيقية 24-bit** و**خطًا مائلًا**، بحيث تظهر الموجّهات المُنسَّقة وتلوين الصياغة وواجهات TUI كما تظهر على سطح المكتب.
- **حافظة OSC 52:** عندما ينسخ برنامج بعيد نصًا (‏`set-clipboard` في tmux، أو نسخ OSC 52 في Vim/Neovim)، فإنه يصل إلى حافظة هاتفك — فيمكنك النسخ من الخادم واللصق في تطبيق محلي.
- على أندرويد وiOS معًا، تُقاس محارف CJK العريضة والإيموجي والمحارف المركّبة (عناقيد المحارف وروابط انعدام العرض) وتُرسَم بشكل صحيح، ويرسم خط **Nerd Font** المضمّن محارف powerline وstarship وdevicon وأيقونات Material Design التي قد يعرضها خط النظام كمربّعات فارغة.

## تكامل الصدفة والصور المضمّنة

- **تكامل الصدفة (OSC 133):** عندما تُصدر صدفتك علامات المحثّات OSC 133، يمكن لـ Mobile SSH التنقّل بين المحثّات، ونسخ مُخرَجات أمر واحد، وتنبيهك عند انتهاء أمر طويل التنفيذ. يعمل على أندرويد وiOS.
- **صور مضمّنة:** ترسم البرامج التي تستخدم بروتوكول رسوميات Kitty الصور مباشرةً داخل الطرفية، على أندرويد وiOS.

## المظهر والمفاتيح

على كلٍّ من أندرويد وiOS، تتيح لك Settings تخصيص الطرفية:

- **الخط:** اختر خط النظام أحادي المسافة أو JetBrains Mono أو Source Code Pro.
- **نظام الألوان:** Default أو Solarized Dark أو Light أو Gruvbox أو Dracula أو Nord — يُطبَّق مباشرةً على اللوحات المفتوحة.
- **صفّ المفاتيح الإضافي:** أضِف المفاتيح وأزِلها وأعِد ترتيبها وأخفِها، وعرّف مفاتيح تسلسل هروب خاصة بك، وأعِد الضبط إلى الإعدادات الافتراضية، مع معاينة مباشرة.
- **حجم سجل التمرير:** حدّد عدد الأسطر التي تحتفظ بها الطرفية.

## التمرير

يوجّه Mobile SSH إيماءات التمرير حسب حالة الطرفية:

- في خرج الصدفة العادي، يمرّر السحب مخزّن التمرير المحلي.
- في تطبيقات الطرفية ذات وضع الفأرة، يرسل التمرير تسلسلات هروب عجلة الفأرة.
- في تطبيقات الشاشة البديلة بدون وضع الفأرة، مثل كثير من جلسات tmux، يدخل التمرير وضع نسخ tmux ويرسل أوامر تمرير بالأسطر.

إذا كتبت أثناء التمرير للخلف، يعود Mobile SSH إلى عرض الطرفية المباشر.

## سلوك tmux

يراقب Mobile SSH أوامر tmux الصادرة للإرفاق وإنشاء جلسة جديدة، مثل:

```bash
tmux attach -t work
tmux a -t work
tmux new -A -s work
```

عند انقطاع الاتصال بينما كنت في tmux، قد يتذكّر التطبيق اسم آخر جلسة tmux لذلك الخادم ويحاول إعادة الإرفاق بعد إعادة الاتصال. إذا لم يُلاحَظ اسم جلسة صريح لكن التطبيق يعرف أنك كنت في جلسة شبيهة بـ tmux بشاشة بديلة، فقد يجرّب `tmux attach` عامًا.

هذا السلوك بأفضل جهد. إذا لم تعد جلسة tmux البعيدة موجودة، تبقى الصدفة البعيدة متاحة.

## Agent alerts

يستطيع Mobile SSH مراقبة خرج الطرفية للجلسة النشطة بحثًا عن أنماط تشير إلى انتظار عامل بعيد للإدخال. عند اكتشاف تطابق — مثل توقف Claude Code أو Codex عند موجّه — يرسل التطبيق إشعارًا مع صوت واهتزاز اختياريَّين.

للضبط:

1. افتح **Settings** من الشاشة الرئيسية.
2. فعّل **Agent alerts**.
3. اختر صوت الإشعار ونمط الاهتزاز.

يُشغَّل التنبيه عبر أي مخرج صوتي نشط، بما في ذلك سماعات الأذن، بحيث تسمعه أثناء مشاهدة مقطع فيديو أو مع قفل الهاتف. يظهر الإشعار حتى عندما يعمل Mobile SSH في الخلفية.

تُطابَق أنماط Agent alerts مع خرج الطرفية المرئي. إذا أخرج أداتك البعيدة سطر موجّه يمكن التعرف عليه (اسم مستخدم، أو `?`، أو سؤال بين أقواس)، فقد يلتقطه التطبيق تلقائيًا. إذا كانت التنبيهات تُطلَق كثيرًا أو لا تُطلَق أبدًا، فاضبط الحساسية في Settings.

## برامج الطرفية بملء الشاشة

لبرامج مثل Vim وless وhtop وأدوات ncurses وأجزاء tmux:

- يمرّ الإدخال مباشرةً إلى البرنامج — فلا يوجد تصحيح تلقائي أو مخزّن اقتراحات للتعارض.
- استخدم صف المفاتيح الإضافية لـ `ESC` والأسهم و`PGUP` و`PGDN`.
- استخدم التكبير بالقرص إذا كان النص صغيرًا جدًا، ثم انتظر قليلًا حتى يستقر حجم الطرفية البعيدة.
